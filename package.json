{
  "name": "hono-project",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bunx nodemon -L --watch src --ext ts,json --exec bun src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "hono": "^4.3.11"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "nodemon": "^3.1.0"
  }
}
